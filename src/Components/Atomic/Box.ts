import { styled } from '../../stitches.config';

export const Box = styled('div', {
  display: 'flex',
  flexDirection: 'column',
  backgroundColor: '$bg',
  color: '$body',
  width: '100%',
  gap: '$default',
});
